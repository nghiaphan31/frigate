# ============================================================================
#                           FRIGATE NVR – FULL CONFIGURATION
# ============================================================================
#  Author:  <your-name>
#  Date:    2025-07-14
#  Purpose: Refactored, fully-annotated, self-contained Frigate configuration
#           * NO YAML anchors – everything is explicit and copy/paste ready
#           * Heavy inline comments explain WHY each value exists, not just WHAT
#           * Clear visual separators so you can jump to any section quickly
#
#  Tested against Frigate 0.15.x (Docker Compose) on an Intel NUC iGPU host
# -----------------------------------------------------------------------------
#  QUICK INDEX
#    1. GLOBAL SETTINGS
#         1.1 MQTT – Home-Assistant event broker
#         1.2 FFMPEG – hardware decoding + record profile
#         1.3 RECORD – retention strategy
#         1.4 SNAPSHOTS – still-image settings
#         1.5 OBJECTS – what to detect and when
#         1.6 DETECTORS – CPU / Coral / GPU
#    2. CAMERAS
#         ├─ allee_sur_le_cote (Ring Floodlight Cam)
#         ├─ jardin_arriere      (Ring Spotlight Cam)
#         ├─ jardin_devant       (Ring Floodlight Cam)
#         ├─ piscine_vue_toit    (Ring Floodlight Cam)
#         ├─ vue_sur_entree_dev  (Ring Spotlight Cam)
#         └─ sonnette_video_wired (Ring Doorbell Pro 2)
#    3. ADVANCED FEATURES
#         3.1 SEMANTIC SEARCH – experiment (beta)
# ----------------------------------------------------------------------------

# ============================================================================
# 1. GLOBAL SETTINGS – APPLY TO EVERY CAMERA UNLESS OVERRIDDEN
# ============================================================================

# ---------------------------------------------------------------------------
# 1.1 MQTT – CONNECTION DETAILS TO YOUR HOME‑ASSISTANT BROKER
# ---------------------------------------------------------------------------
mqtt:
  host: 192.168.50.125         # IP/hostname of the MQTT broker (Mosquitto on HA)
  user: mosquito             # Frigate‑only MQTT username
  password: mosquito         # …and its matching password
  topic_prefix: frigate      # Change if you plan multiple Frigate instances

# ---------------------------------------------------------------------------
# 1.2 FFMPEG – HARDWARE ACCELERATION & OUTPUT PROFILES
# ---------------------------------------------------------------------------
ffmpeg:
  # The Intel NUC’s iGPU is capable of decoding H.264 streams via VA‑API.
  # This offloads work from CPU so detections stay real‑time.
  hwaccel_args: preset-vaapi

  output_args:
    # The built‑in record preset below:
    #   • copies the original video (no re‑encode) – fast & lossless
    #   • transcodes audio ➜ AAC – to satisfy browsers that reject G.711/PCMU
    record: preset-record-generic-audio-aac

# ---------------------------------------------------------------------------
# 1.3 RECORD – HOW LONG TO KEEP VIDEO CLIPS
# ---------------------------------------------------------------------------
record:
  enabled: false             # DISABLED for training data mode — maximize detection rate
  retain:
    days: 3                  # Purge any clip older than X days
    mode: motion             # "all" = continuous 24/7; "motion" = only events

# ---------------------------------------------------------------------------
# 1.4 SNAPSHOTS – STILL IMAGES FOR NOTIFICATIONS / ARCHIVE
# ---------------------------------------------------------------------------
snapshots:
  enabled: true              # Global toggle; camera blocks can override
  timestamp: true            # Burn date & time onto the PNG/JPEG
  bounding_box: true         # Draw red box around detected object(s)
  clean_copy: true           # If true, save a clean copy (no annotations) for Frigate+
  crop: false                # True ➜ only keep the boxed area – saves space
  retain:
    default: 0               # Keep *non‑person* snaps for 0 days (discard)
    objects:
      person: 7              # Retain person snaps for one week

# ============================================================================
# ⚠️  FRIGATE+ TRAINING MODE – ULTRA-PERMISSIVE DETECTION ⚠️
# This config is designed to maximize detections for Frigate+ training data.
# DO NOT use for normal operation — too many false positives!
# ============================================================================
objects:
  track:
    - person                 # Detect only human beings by default
  filters:
    person:
      min_area: 50          # TRAINING MODE: very tiny
      min_score: 0.05        # TRAINING MODE: accept low-confidence detections
      threshold: 0.05        # TRAINING MODE: trigger events easily
      max_area: 300000       # Larger max to catch close-up silhouettes

# ---------------------------------------------------------------------------
# 1.6 DETECTORS – CHOOSE THE HARDWARE INFERENCE ACCELERATOR
# ---------------------------------------------------------------------------
#  Options:
#    • type: cpu       – universal, slowest
#    • type: edgetpu   – Google Coral USB / PCIe (fast, low power)
#    • type: openvino  – Intel GPU/OpenVINO (experimental)
#    • type: tensorrt  – NVIDIA GPU (fast, power hungry)
# ---------------------------------------------------------------------------
detectors:
  cpu1:
    type: cpu              # Using CPU only – upgrade to Coral for speed

# ---------------------------------------------------------------------------
# 1.7 MOTION PRE-FILTER (BEFORE ML) – default pre-filter for all cameras
# ---------------------------------------------------------------------------
motion:
  # Plus haut ➜ moins sensible. 26 reste assez sensible pour l’allumage de lumière,
  # mais filtre déjà pas mal de micro-mouvements.
  threshold: 5             # TRAINING MODE: ultra-sensitive (was 10)

  # Rejette les tout petits blobs (feuilles isolées, bruit).
  contour_area: 50

  # Lissage temporel : évite de “sauter” sur un changement ultra ponctuel
  frame_alpha: 0.2
  delta_alpha: 0.3

  # Garde ça, ça aide beaucoup de nuit sans trop pénaliser le jour
  improve_contrast: true

# --- Frigate audio section tuned for security-only events -------------------
audio:
  enabled: false             # DISABLED for training data mode — focus on video detections only
  # Start slightly conservative; raise if you get too many false events
  # The louder the ambient noise, the higher you set min_volume
  #   ≈200  very sensitive (quiet room)
  #   ≈350  typical living-room background
  #   ≈700+ noisy street-facing cam
  min_volume: 350

  # Omit 'listen:' at first so you see everything Frigate recognizes.
  # After you confirm events appear, reintroduce 'listen:' with just the labels you want.  

#  # Frigate will ONLY publish events whose label is in this list ----------------
#  listen:
#    # ── Group 1: forced-entry / violence ───────────────────────────────────────
#    - glass_breaking        # Window/door smash
#    - gunshot_gunfire       # Handgun or rifle report
#    - explosion             # Large bang (can include fireworks)
#    - scream                # Human distress
#    - yell                  # Aggressive shouting
#
#    # ── Group 2: life-safety alarms ────────────────────────────────────────────
#    - smoke_alarm           # Residential smoke detector pattern
#    - fire_alarm            # Commercial horn-strobe tone
#    - carbon_monoxide_alarm # CO detector beeps
#    - car_alarm             # Vehicle alarm siren
#    - siren                 # Police / ambulance siren nearby


# ============================================================================
# 2. CAMERA DEFINITIONS – ONE BLOCK PER PHYSICAL CAMERA
# ============================================================================

cameras:

  # ========================================================================
  # CAMERA: ALLEE_SUR_LE_COTE — Driveway side view (Reolink Duo 3 Wi-Fi ETH)
  # - Substream → detection (ultra-wide 1536x432, light for ML)
  # - Main stream → recording + audio
  # - Primary outdoor security feed (with mic + speaker)
  # ========================================================================
  # allee_sur_le_cote:

    # ---------------------------- STREAM INPUTS -----------------------------
    # ffmpeg:
      # hwaccel_args: []   # Uncomment if VA-API crashes on detect
      # inputs:
        # - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_allee_cote_sub
          # roles: [detect]
        # - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_allee_cote_main
          # roles: [record, audio]

    # --------------------------- AUDIO (RECORD) -----------------------------
    # audio:
      # enabled: true              # Keep mic channel in recordings

    # --------------------------- MQTT SNAPSHOTS -----------------------------
    # # Allows Home Assistant (or others) to request on-demand snapshots via MQTT.
    # mqtt:
      # enabled: true              # Enable MQTT snapshots
      # timestamp: true            # Add timestamp overlay
      # bounding_box: true         # Draw bounding boxes
      # crop: false                # Send whole frame (not cropped to object)

    # --------------------------- DETECTION CORE -----------------------------
    # detect:
      # width: 1536
      # height: 432
      # fps: 5                     # Balanced; raise if NUC load is fine

    # ------------------------------ OBJECTS --------------------------------
    # objects:
    #   track: [person]            # Focus only on people
    #   filters:
    #     person:
    #       min_area: 50          # TRAINING MODE
    #       max_area: 200000       # Prevent runaway boxes from broken frames
    #       min_ratio: 0.01        # TRAINING MODE: extreme shape flexibility
    #       max_ratio: 10         # TRAINING MODE: extreme shape flexibility
    #       threshold: 0.05      # TRAINING MODE

    # # ------------------------------ SNAPSHOTS -------------------------------
    # snapshots:
    #   enabled: true              # Standard snapshot behaviour

    # # --------------------------- ZONES & BEHAVIOUR --------------------------
    # zones:
    #   # ---- 'prive' — Private driveway (immediate alerts) ------------------
    #   prive:
    #     coordinates: 
    #       0.276,1,0,1,0,0.539,0.064,0.645,0.104,0.361,0.102,0.329,0.092,0.31,0.131,0.248,0.225,0.112,0.323,0,0.478,0,0.65,0,0.693,0.053,0.762,0.129,0.827,0.22,0.887,0.323,0.908,0.362,0.925,0.332,0.95,0.331,0.963,0.395,0.972,0.439,0.989,0.588,1,0.573,0.999,0.996
    #     loitering_time: 0        # Trigger immediately when person enters
    #     inertia: 3               # Hold event for 3s after leaving
    #     objects: [person]
    #     filters:
    #       person:
    #         threshold: 0.05      # Training mode: very permissive
    #         min_area: 50       # Training mode: capture tiny boxes
    #         min_ratio: 0.15      # Allow tall, narrow boxes
    #         max_ratio: 4.0       # Allow more square boxes

    #   # ---- 'rodage' — Fence line / loitering detection --------------------
    #   rodage:
    #     coordinates: 0.075,0.473,0.094,0.335,0.101,0.343,0.064,0.623,0.056,0.612
    #     loitering_time: 10       # Require ≥10s loitering before event
    #     inertia: 3
    #     objects: [person]
    #     filters:
    #       person:
    #         threshold: 0.05      # Training mode: very permissive
    #         min_area: 50       # Training mode: capture tiny boxes
    #         min_ratio: 0.01        # TRAINING MODE: extreme shape flexibility
    #         max_ratio: 10         # TRAINING MODE: extreme shape flexibility

    # # ----------------------------- REVIEW RULES -----------------------------
    # review:
    #   alerts:
    #     required_zones: prive    # Only alert if event entered private zone
    #   detections:
    #     required_zones: rodage   # Keep events when someone lingers at rodage

  allee_sur_le_cote_left:

    # ---------------------------- STREAM INPUTS -----------------------------
    ffmpeg:
      # hwaccel_args: []   # Uncomment if VA-API crashes on detect
      inputs:
        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_allee_cote_sub_left
          roles: [detect]
        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_allee_cote_main
          roles: [record, audio]

    # --------------------------- AUDIO (RECORD) -----------------------------
    audio:
      enabled: true              # Keep mic channel in recordings

    # --------------------------- MQTT SNAPSHOTS -----------------------------
    # Allows Home Assistant (or others) to request on-demand snapshots via MQTT.
    mqtt:
      enabled: true              # Enable MQTT snapshots
      timestamp: true            # Add timestamp overlay
      bounding_box: true         # Draw bounding boxes
      crop: false                # Send whole frame (not cropped to object)

    # --------------------------- DETECTION CORE -----------------------------
    detect:
      width: 768
      height: 432
      fps: 5                     # Balanced; raise if NUC load is fine

    # ------------------------------ OBJECTS --------------------------------
    objects:
      track: [person]            # Focus only on people
      filters:
        person:
          min_area: 50          # TINY — training mode capture
          max_area: 200000       # Prevent runaway boxes
          min_ratio: 0.01        # TRAINING MODE: extreme shape flexibility
          max_ratio: 10         # TRAINING MODE: extreme shape flexibility
          threshold: 0.05        # VERY LOW — training mode

    # ------------------------------ SNAPSHOTS -------------------------------
    snapshots:
      enabled: true              # Standard snapshot behaviour

    # --------------------------- ZONES & BEHAVIOUR --------------------------
    zones:
      # ---- 'prive' — Private driveway (immediate alerts) ------------------
      prive:
        coordinates: 
          1,1,1,0.002,0.703,0.002,0.506,0.112,0.496,0.124,0.486,0.12,0.464,0.138,0.322,0.239,0.254,0.296,0.244,0.32,0.234,0.329,0.226,0.336,0.247,0.363,0.155,0.694,0,0.568,0,1
        loitering_time: 0        # Trigger immediately when person enters
        inertia: 3               # Hold event for 3s after leaving
        objects: person
        filters:
          person:
            threshold: 0.05      # Training mode: very permissive
            min_area: 50       # Training mode: capture tiny boxes
            min_ratio: 0.01       # TRAINING MODE: extreme shape flexibility
            max_ratio: 10         # TRAINING MODE: extreme shape flexibility

      # ---- 'rodage' — Fence line / loitering detection --------------------
      rodage:
        coordinates: 0.231,0.382,0.214,0.37,0.126,0.653,0.151,0.665
        loitering_time: 10       # Require ≥10s loitering before event
        inertia: 3
        objects: person
        filters:
          person:
            threshold: 0.05      # Training mode: very permissive
            min_area: 50       # Training mode: capture tiny boxes
            min_ratio: 0.01       # TRAINING MODE: extreme shape flexibility
            max_ratio: 10         # TRAINING MODE: extreme shape flexibility

    # ----------------------------- REVIEW RULES -----------------------------
    # review:
    #   alerts:
    #     required_zones: prive    # Only alert if event entered private zone
    #   detections:
    #     required_zones: rodage   # Keep events when someone lingers at rodage

    # motion:
    #   mask: 
    #     0.587,0.002,0,0,0,0.533,0.125,0.642,0.214,0.35,0.199,0.315,0.207,0.292,0.308,0.2,0.398,0.127,0.41,0.117,0.422,0.097


  allee_sur_le_cote_right:

    # ---------------------------- STREAM INPUTS -----------------------------
    ffmpeg:
      # hwaccel_args: []   # Uncomment if VA-API crashes on detect
      inputs:
        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_allee_cote_sub_right
          roles: [detect]
#        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_allee_cote_main
#          roles: [record, audio]

    # --------------------------- AUDIO (RECORD) -----------------------------
    audio:
      enabled: false              # Keep mic channel in recordings

    # --------------------------- MQTT SNAPSHOTS -----------------------------
    # Allows Home Assistant (or others) to request on-demand snapshots via MQTT.
    mqtt:
      enabled: true              # Enable MQTT snapshots
      timestamp: true            # Add timestamp overlay
      bounding_box: true         # Draw bounding boxes
      crop: false                # Send whole frame (not cropped to object)

    # --------------------------- DETECTION CORE -----------------------------
    detect:
      width: 768
      height: 432
      fps: 5                     # Balanced; raise if NUC load is fine

    # ------------------------------ OBJECTS --------------------------------
    objects:
      track: [person]            # Focus only on people
      filters:
        person:
          min_area: 50          # TINY — training mode capture
          max_area: 200000       # Prevent runaway boxes
          min_ratio: 0.01        # TRAINING MODE: extreme shape flexibility
          max_ratio: 10         # TRAINING MODE: extreme shape flexibility
          threshold: 0.05        # VERY LOW — training mode

    # ------------------------------ SNAPSHOTS -------------------------------
    snapshots:
      enabled: true              # Standard snapshot behaviour

    # --------------------------- ZONES & BEHAVIOUR --------------------------
    zones:
      # ---- 'prive' — Private driveway (immediate alerts) ------------------
      prive:
        coordinates: 
          1,1,0,1,0,0,0.299,0,0.498,0.097,0.63,0.177,0.748,0.274,0.823,0.349,0.849,0.338,0.877,0.322,0.907,0.299,0.926,0.288,0.939,0.327,0.946,0.359,0.926,0.379,1,0.582,1,0.605
        loitering_time: 0        # Trigger immediately when person enters
        inertia: 3               # Hold event for 3s after leaving
        objects: person
        filters:
          person:
            threshold: 0.05      # Training mode: very permissive
            min_area: 50       # Training mode: capture tiny boxes
            min_ratio: 0.01       # TRAINING MODE: extreme shape flexibility
            max_ratio: 10         # TRAINING MODE: extreme shape flexibility

      # ---- 'rodage' — Fence line / loitering detection --------------------
    # review:
    #   alerts:
    #     required_zones: prive    # Only alert if event entered private zone
    # motion:
    #   mask: 
    #     0.464,0.011,0.499,0,0.587,0,1,0.004,1,0.51,0.971,0.301,0.948,0.188,0.952,0.104,0.913,0.027,0.765,0.028,0.83,0.173,0.85,0.223,0.868,0.258,0.832,0.294,0.694,0.17,0.665,0.145,0.623,0.113


  jardin_arriere:

    # ---------------------------- STREAM INPUTS -----------------------------
    ffmpeg:
      # hwaccel_args: []   # Uncomment if VA-API crashes on detect
      inputs:
        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_vue_nord_sur_maison_sub
          roles: [detect]
        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_vue_nord_sur_maison_main
          roles: [record, audio]

    # --------------------------- AUDIO (RECORD) -----------------------------
    audio:
      enabled: true              # Keep mic channel in recordings

    # --------------------------- DETECTION CORE -----------------------------
    detect:
      width: 640
      height: 360
      fps: 4                     # Raise from 2 → 4 for more frames without overloading

    # ------------------------------ OBJECTS --------------------------------
    objects:
      track: [person]
      filters:
        person:
          min_area: 50          # TINY — training mode capture
          max_area: 150000       # Safety cap
          min_ratio: 0.01        # TRAINING MODE: extreme shape flexibility
          max_ratio: 10         # TRAINING MODE: extreme shape flexibility
          threshold: 0.05        # VERY LOW — training mode

    # ------------------------------ SNAPSHOTS -------------------------------
    snapshots:
      enabled: true              # Use global snapshot policy

    # ---------------------------- MOTION FILTER -----------------------------
    # motion:
    #   mask:
    #     - 0.091,0.149,0.081,0.315,0.103,0.347,0.231,0.336,0.245,0.301,0.214,0.274,0.215,0.221,0.201,0.168,0.178,0.143,0.158,0.122,0.13,0.12
    #     - 0.429,0.073,0.45,0.078,0.471,0.126,0.47,0.158,0.453,0.188,0.429,0.179,0.412,0.143,0.398,0.115
    # --------------------------- ZONES & BEHAVIOUR --------------------------
    zones:
      # ---- 'prive' — Play area + shed ------------------------------------
      prive:
        coordinates: 0,1,0.002,0.354,0.239,0.343,0.26,0.044,0.287,0.071,0.304,0.092,
          0.314,0.115,0.524,0.124,0.537,0.166,0.528,0.179,0.523,0.383, 0.741,0.382,0.738,0.338,1,0.364,0.997,0.997
        loitering_time: 0        # Immediate alert if person enters
        inertia: 3
        objects: [person]
        filters:
          person:
            threshold: 0.05      # Training mode: very permissive
            min_area: 50        # Training mode: capture tiny boxes
            min_ratio: 0.01       # TRAINING MODE: extreme shape flexibility
            max_ratio: 10         # TRAINING MODE: extreme shape flexibility

    # ----------------------------- REVIEW RULES -----------------------------
    # review:
    #   alerts:
    #     required_zones: prive    # Only alert if in private play/shed area
    #   detections:
    #     required_zones: prive    # Only keep events when in this area

  # ========================================================================
  # CAMERA: VUE_ENTREE — (Reolink Doorbell POE)
  # - Substream → detection (640x360, light for ML)
  # - Main stream → recording + audio
  # ========================================================================
  vue_entree:

    # ---------------------------- STREAM INPUTS -----------------------------
    ffmpeg:
      # hwaccel_args: []   # Uncomment if VA-API crashes on detect
      inputs:
        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_vue_entree_sub
          roles: [detect]
        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_vue_entree_main
          roles: [record, audio]

    # --------------------------- AUDIO (RECORD) -----------------------------
    audio:
      enabled: true              # Keep mic channel in recordings

    # --------------------------- DETECTION CORE -----------------------------
    detect:
      width: 640
      height: 480
      fps: 4                     # Raise from 2 → 4 for more frames without overloading

    # ------------------------------ OBJECTS --------------------------------
    objects:
      track: [person]
      filters:
        person:
          min_area: 50         # Adjusted for smaller frame; ~20x60 pixels
          max_area: 150000       # Safety cap
          min_ratio: 0.01        # TRAINING MODE: extreme shape flexibility
          max_ratio: 10         # TRAINING MODE: extreme shape flexibility
          threshold: 0.05        # Slightly more permissive (bust-only views of kids)

    # ------------------------------ SNAPSHOTS -------------------------------
    snapshots:
      enabled: true              # Use global snapshot policy

    # ---------------------------- MOTION FILTER -----------------------------
    # motion:
    #   mask: 
    #     0.623,0.08,0.605,0.109,0.602,0.175,0.633,0.181,0.668,0.186,0.678,0.157,0.678,0.109,0.655,0.081

    # --------------------------- ZONES & BEHAVIOUR --------------------------
    zones:
      # ---- 'prive' ------------------------------------
      prive:
        coordinates: 
          0,0.735,0,0.488,0.31,0.538,0.308,0,0.456,0,0.685,0,0.731,0.154,0.809,0.204,0.823,0.275,0.836,0.361,0.85,0.487,1,0.491,1,1,0.73,1,0.741,0.953,0.676,0.893,0.515,0.96,0.363,0.975,0.188,0.956,0.036,0.89,0.03,0.744
        loitering_time: 0        # Immediate alert if person enters
        inertia: 3
        objects: person
        filters:
          person:
            threshold: 0.05      # Relaxed for partial/child-sized detections
            min_area: 50        # Smaller minimum box allowed
            min_ratio: 0.01       # TRAINING MODE: extreme shape flexibility
            max_ratio: 10         # TRAINING MODE: extreme shape flexibility

    # ----------------------------- REVIEW RULES -----------------------------
    # review:
    #   alerts:
    #     required_zones: prive    # Only alert if in private play/shed area
    #   detections:
    #     required_zones: prive    # Only keep events when in this area


# ========================================================================
# CAMERA: JARDIN_DEVANT2 — Front garden / road (Reolink Duo 3)
# - Substream used for detection (ultra-wide 1536x432)
# - Main stream used for recording + audio
# - Faces a public road → stricter zoning, loitering band, and privacy rules
# ========================================================================
#   jardin_devant2:
#     # ---------------------------- STREAM INPUTS -----------------------------
#     ffmpeg:
#       # Tip: if VA-API crashes on detect for Duo 3, uncomment to force software decode ONLY for this cam:
#       # hwaccel_args: []
#       inputs:
#         # Low-res substream → detection (lighter, faster)
#         - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_jardin_devant_sub
#           roles: [detect]
#         # High-res main stream → recording (and audio if present)
#         - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_jardin_devant_main
#           roles: [record, audio]

#     # --------------------------- AUDIO (RECORD) -----------------------------
#     audio:
#       enabled: true            # Keep mic channel in recordings (useful for later analytics/rules)

#     # --------------------------- DETECTION CORE -----------------------------
#     detect:
#       width: 1536              # Match substream width (ultra-wide)
#       height: 432              # Match substream height
#       fps: 4                   # Balanced: enough chances to catch a good pose, still light on CPU

#     # ------------------------------ OBJECTS --------------------------------
#     objects:
#       track:
#         - person               # Only track people (ignore vehicles for privacy)
#       filters:
#         person:
#           # Global person filter (applies everywhere unless a zone overrides it)
#           min_area: 50       # TINY — catch far/distant silhouettes (training mode)
#           max_area: 200000     # Safety cap for broken frames/huge blobs
#           min_ratio: 0.01        # TRAINING MODE: extreme shape flexibility      # Allow tall/narrow boxes (bust-only, seen from above)
#           max_ratio: 3.5       # Disallow ultra-thin poles / extreme aspect boxes
#           min_score: 0.05      # TRAINING MODE: accept low-confidence new tracks
#           threshold: 0.05      # TRAINING MODE: base confidence; zones can override

#           # Optional: keep tracks alive when briefly occluded (bush/wall edge)
#           # max_disappeared: 30

#     # ------------------------------ SNAPSHOTS -------------------------------
#     snapshots:
#       enabled: true            # Standard snapshot behaviour (uses main stream quality if configured globally)

#     # --------------------------- ZONES & BEHAVIOUR --------------------------
#     zones:

#       # ---- 'prive' — Inside the fence (your private area) ------------------
#       # More permissive: person may be partially seen (bust/edge/occluded).
#       prive:
#         coordinates:
#           0,0.407,0.081,0.745,0.17,0.5,0.196,0.417,0.226,0.337,0.24,0.463,0.258,0.488,0.286,0.499,0.325,0.512,0.402,0.55,0.646,0.624,0.667,0.397,0.671,0.466,0.683,0.476,0.691,0.483,0.778,0.534,0.788,0.469,0.813,0.563,0.89,0.801,0.929,0.894,0.971,0.937,1,0.827,1,1,0,1
#         loitering_time: 0      # Immediate alert when a person enters
#         inertia: 3             # Keep event open for 3 s after exit (avoid flicker)
#         objects: person
#         filters:
#           person:
#             # Night/bust-only friendly thresholds scoped to this private zone
#             threshold: 0.05    # TRAINING MODE: very permissive
#             min_area: 50     # TRAINING MODE: permit smaller boxes
#             min_ratio: 0.15    # Allow very tall/narrow boxes (seen from above)
#             max_ratio: 4.0     # Allow squarer boxes (3/4 body, arm positions)

#       # ---- 'rodage' — Sidewalk/approach band (outside fence) ---------------
#       # Stricter behaviour: only alert if someone lingers (loiters) here.
#       rodage:
#         coordinates:
#           0.228,0.298,0.261,0.197,0.436,0.215,0.611,0.277,0.664,0.302,0.665,0.367,0.678,0.376,0.69,0.41,0.757,0.449,0.768,0.394,0.782,0.445,0.79,0.453,0.776,0.526,0.678,0.467,0.671,0.404,0.661,0.417,0.642,0.599,0.513,0.564,0.249,0.463,0.24,0.397
#         loitering_time: 10     # Require ≥10 s inside before raising event
#         inertia: 3             # Debounce exit
#         objects: person
#         filters:
#           person:
#             # TRAINING MODE: very permissive
#             threshold: 0.05
#             min_area: 50
#             min_ratio: 0.01        # TRAINING MODE: extreme shape flexibility
#             max_ratio: 10         # TRAINING MODE: extreme shape flexibility

#     # ----------------------------- REVIEW RULES -----------------------------
#     # These are your policy rules for what gets alerted vs merely recorded.
#     review:
#       alerts:
#         required_zones: prive   # Push notifications only if the event entered 'prive'
#       detections:
#         required_zones: rodage  # Keep events when someone loiters at 'rodage'

  jardin_devant_left:
    # ---------------------------- STREAM INPUTS -----------------------------
    ffmpeg:
      # Tip: if VA-API crashes on detect for Duo 3, uncomment to force software decode ONLY for this cam:
      # hwaccel_args: []
      inputs:
        # Low-res substream → detection (lighter, faster)
        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_jardin_devant_main_left
          roles: [detect]
        # High-res main stream → recording (and audio if present)
        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_jardin_devant_main
          roles: [record]

    # --------------------------- AUDIO (RECORD) -----------------------------
    audio:
      enabled: false            # Keep mic channel in recordings (useful for later analytics/rules)

    # --------------------------- DETECTION CORE -----------------------------
    detect:
      width: 2048              # Match substream width (ultra-wide)
      height: 1152              # Match substream height
      fps: 5                   # Balanced: enough chances to catch a good pose, still light on CPU
      max_disappeared: 40   # ≈ 10 s à 5 fps (50 frames sans détection avant de “perdre” l’objet)


    # ---------------------------- MOTION GATING -----------------------------
    # TRAINING MODE: relaxed to maximize detection sensitivity
    motion:
      threshold: 5           # TRAINING MODE: ultra-sensitive (was 10)
      contour_area: 50         # TRAINING MODE: global default
      frame_alpha: 0.2
      delta_alpha: 0.3          # TRAINING MODE: global default
      improve_contrast: true
      # mask DISABLED for training mode — catch all motion
      # mask: 0,0.504,0,0,0.015,0,0.999,0,0.99,0.016,0.556,0.067,0.511,0.165,0.394,0.775,0.199,0.651
    objects:
      track:
        - person
      filters:
        person:
          # TRAINING MODE: very permissive for far/distal figures
          min_area: 50      # TRAINING MODE: catch small figures
          max_area: 80000
          min_score: 0.05     # TRAINING MODE: accept low-confidence tracks
          threshold: 0.05     # TRAINING MODE: much lower
          min_ratio: 0.01        # TRAINING MODE: extreme shape flexibility (was 0.05)
          max_ratio: 10         # TRAINING MODE: extreme shape flexibility (was 5)
      # mask DISABLED for training mode
      # mask: 0.002,0,1,0,1,0.026,0.768,0.04,0.568,0.085,0.546,0.129,0.511,0.276,0.484,0.425,0.444,0.649,0.412,0.811,0,0.556
    # ------------------------------ SNAPSHOTS -------------------------------
    snapshots:
      enabled: true            # Standard snapshot behaviour (uses main stream quality if configured globally)

    # --------------------------- ZONES & BEHAVIOUR --------------------------
    zones:

      # ---- 'prive' — Inside the fence (your private area) ------------------
      # More permissive: person may be partially seen (bust/edge/occluded).
      prive:
        coordinates: 0,1,1,1,1,0.285,0.65,0.296,0.576,0.204,0.47,0.865,0,0.561
        loitering_time: 0      # Immediate alert when a person enters
        inertia: 3             # Keep event open for 3 s after exit (avoid flicker)
        objects: person
        filters:
          person:
            # Encore plus permissif à l’intérieur de ta propriété
            threshold: 0.05      # TRAINING MODE
            min_area: 50       # TRAINING MODE
            min_ratio: 0.01       # TRAINING MODE: extreme shape flexibility (was 0.25)
            max_ratio: 10         # TRAINING MODE: extreme shape flexibility (was 5)

      # ---- 'rodage' — Sidewalk/approach band (outside fence) ---------------
      # Stricter behaviour: only alert if someone lingers (loiters) here.
      rodage:
        coordinates: 
          1,0.034,0.998,0.28,0.65,0.297,0.575,0.191,0.469,0.858,0.416,0.821,0.539,0.136,0.582,0.092,0.793,0.046
        loitering_time: 10     # Require ≥10 s inside before raising event
        inertia: 3             # Debounce exit
        objects: person
        filters:
          person:
            # Plus strict que 'prive' pour éviter de spammer sur le trottoir
            threshold: 0.05      # TRAINING MODE
            min_area: 50        # TRAINING MODE
            min_ratio: 0.01       # TRAINING MODE: extreme shape flexibility (was 0.10)
            max_ratio: 10         # TRAINING MODE: extreme shape flexibility (was 5.0)

    #     # ----------------------------- REVIEW RULES -----------------------------
    # TRAINING MODE: no review restrictions — capture all detections

  jardin_devant_right:
    # ---------------------------- STREAM INPUTS -----------------------------
    ffmpeg:
      # Tip: if VA-API crashes on detect for Duo 3, uncomment to force software decode ONLY for this cam:
      # hwaccel_args: []
      inputs:
        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_jardin_devant_main_right
          roles: [detect]
        # High-res main stream → recording (and audio if present)
#        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_jardin_devant_main
#          roles: [record, audio]

    # --------------------------- AUDIO (RECORD) -----------------------------
    audio:
      enabled: false            # Keep mic channel in recordings (useful for later analytics/rules)

    # --------------------------- DETECTION CORE -----------------------------
    detect:
      width: 2048              # Match substream width (ultra-wide)
      height: 1152              # Match substream height
      fps: 5                   # Balanced: enough chances to catch a good pose, still light on CPU
      max_disappeared: 40   # ≈ 10 s à 5 fps (50 frames sans détection avant de “perdre” l’objet)


    # ---------------------------- MOTION GATING -----------------------------
    # TRAINING MODE: relaxed to maximize detection sensitivity
    motion:
      threshold: 5           # TRAINING MODE: ultra-sensitive (was 10)
      contour_area: 50         # TRAINING MODE: global default
      frame_alpha: 0.2
      delta_alpha: 0.3          # TRAINING MODE: global default
      improve_contrast: true
      # mask DISABLED for training mode — catch all motion
      # mask:
      #   - 0,0.032,0.042,0.036,0.188,0.058,0.356,0.093,0.346,0.141,0.377,0.161,0.497,0.204,0.506,0.227,0.542,0.241,0.664,0.784,1,0.556,1,0,0,0
      #   - 0,0.391,0.074,0.487,0.071,0.563,0.05,0.621,0.045,0.644,0.033,0.676,0,0.687

    objects:
      track:
        - person
      filters:
        person:
          min_area: 50        # TRAINING MODE
          max_area: 250000

          # Forme humaine, mais pas trop restrictive vu la vue plongeante
          min_ratio: 0.01       # TRAINING MODE: extreme shape flexibility (was 0.05)
          max_ratio: 10         # TRAINING MODE: extreme shape flexibility (was 5.0)

          # Confiance : plus souple que ce qu’on avait mis
          threshold: 0.05      # TRAINING MODE
          min_score: 0.05     # TRAINING MODE
      # mask DISABLED for training mode
      # mask: 0.002,0,0.99,0,1,0.598,0.669,0.812,0.536,0.23,0.504,0.235,0.488,0.193,0.349,0.136,0.353,0.085,0.002,0.03

    # ------------------------------ SNAPSHOTS -------------------------------
    snapshots:
      enabled: true            # Standard snapshot behaviour (uses main stream quality if configured globally)

    # --------------------------- ZONES & BEHAVIOUR --------------------------
    zones:

      # ---- 'prive' — Inside the fence (your private area) ------------------
      # More permissive: person may be partially seen (bust/edge/occluded).
      prive:
        coordinates: 
          0,1,0,0.267,0.297,0.288,0.327,0.214,0.495,0.271,0.526,0.269,0.646,0.827,0.65,0.832,1,0.602,1,0.991,1,1
        loitering_time: 2       # 0 → 2 s avant de lever l’évènement
        inertia: 3
        objects: person
        filters:
          person:
            # Légèrement plus strict que la caméra, mais cohérent
            threshold: 0.05      # TRAINING MODE
            min_area: 50       # TRAINING MODE
            min_ratio: 0.01       # TRAINING MODE: extreme shape flexibility (was 0.20)
            max_ratio: 10         # TRAINING MODE: extreme shape flexibility (was 2.0)

      # ---- 'rodage' — Sidewalk/approach band (outside fence) ---------------
      # Stricter behaviour: only alert if someone lingers (loiters) here.
      rodage:
        coordinates: 
          0,0.255,0,0.042,0.217,0.067,0.346,0.099,0.339,0.147,0.494,0.207,0.508,0.241,0.54,0.239,0.667,0.819,0.65,0.837,0.529,0.283,0.523,0.273,0.501,0.273,0.337,0.214,0.3,0.287,0.182,0.28
        loitering_time: 10     # Require ≥10 s inside before raising event
        inertia: 3             # Debounce exit
        objects: person
        filters:
          person:
            # Plus strict que 'prive' pour le trottoir
            threshold: 0.05      # TRAINING MODE
            min_area: 50
            min_ratio: 0.01       # TRAINING MODE: extreme shape flexibility (was 0.20)
            max_ratio: 10         # TRAINING MODE: extreme shape flexibility (was 2.0)

    # ----------------------------- REVIEW RULES -----------------------------
    # TRAINING MODE: no review restrictions — capture all detections


  # ========================================================================
  # CAMERA: PISCINE_VUE_TOIT — Pool rooftop view (Reolink Duo 3 Wi-Fi ETH)
  # - Substream → detection (ultra-wide 1536x432, light for ML)
  # - Main stream → recording + audio
  # ========================================================================
  #   piscine_vue_toit:

    # ---------------------------- STREAM INPUTS -----------------------------
    # ffmpeg:
      # hwaccel_args: []   # Uncomment if VA-API crashes on detect
      # inputs:
        # - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_vue_sud_sub
          # roles: [detect]
        # - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_vue_sud_main
          # roles: [record, audio]

    # --------------------------- AUDIO (RECORD) -----------------------------
    # audio:
      # enabled: true              # Keep mic channel in recordings

    # --------------------------- DETECTION CORE -----------------------------
    # detect:
      # width: 1536
      # height: 432
      # fps: 4                     # Slightly higher than 2 for more chances to catch poses

    # ------------------------------ OBJECTS --------------------------------
    # objects:
      # track: [person]
      # filters:
        # person:
          # min_area: 50         # TRAINING MODE: catch small/distant figures
          # max_area: 200000
          # min_ratio: 0.01        # TRAINING MODE: extreme shape flexibility
          # max_ratio: 10         # TRAINING MODE: extreme shape flexibility
          # threshold: 0.05        # TRAINING MODE: very permissive

    # ------------------------------ SNAPSHOTS -------------------------------
    # snapshots:
      # enabled: true              # Use global settings for retention

    # --------------------------- ZONES & BEHAVIOUR --------------------------
    # zones:
      # ---- 'prive' — Pool rooftop area (full frame) ----------------------
      # prive:
        # coordinates: 0.097,1,0.178,0.715,0.249,0.38,0.245,0.3,0.243,0.249,0.239,0.199,
          # 0.279,0.11,0.296,0.077,0.308,0.045,0.316,0.059,0.37,0.03,0.374,0.059, 0.387,0.059,0.4,0.059,0.405,0.021,0.495,0.021,0.534,0.098,0.587,0.257,
          # 0.615,0.338,0.629,0.406,0.642,0.445,0.652,0.454,0.666,0.453,0.681,0.499,
          # 0.691,0.576,0.703,0.609,0.713,0.611,0.796,0.006,0.999,0,0.998,0.994
        # loitering_time: 0        # Immediate alert if person appears
        # inertia: 3               # Hold event for 3s after leaving
        # objects: [person]
        # filters:
          # person:
            # threshold: 0.05      # TRAINING MODE: very permissive
            # min_area: 50       # TRAINING MODE: allow smaller boxes
            # min_ratio: 0.01      # TRAINING MODE: extreme shape flexibility
            # max_ratio: 10         # TRAINING MODE: extreme shape flexibility

    # ----------------------------- REVIEW RULES -----------------------------
    # review:
      # alerts:
        # required_zones: prive    # Only alert if event is inside the pool area
      # detections:
        # required_zones: prive    # Only keep detections when inside pool area

  piscine_vue_toit_left:

    # ---------------------------- STREAM INPUTS -----------------------------
    ffmpeg:
      # hwaccel_args: []   # Uncomment if VA-API crashes on detect
      inputs:
        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_vue_sud_sub_left
          roles: [detect]
        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_vue_sud_main
          roles: [record, audio]

    # --------------------------- AUDIO (RECORD) -----------------------------
    audio:
      enabled: true              # Keep mic channel in recordings

    # --------------------------- DETECTION CORE -----------------------------
    detect:
      width: 768
      height: 432
      fps: 4                     # Slightly higher than 2 for more chances to catch poses

    # ------------------------------ OBJECTS --------------------------------
    objects:
      track: [person]
      filters:
        person:
          min_area: 50         # TRAINING MODE: catch small/distant figures
          max_area: 200000
          min_ratio: 0.01        # TRAINING MODE: extreme shape flexibility
          max_ratio: 10         # TRAINING MODE: extreme shape flexibility
          threshold: 0.05        # TRAINING MODE: very permissive

    # ------------------------------ SNAPSHOTS -------------------------------
    snapshots:
      enabled: true              # Use global settings for retention

    # --------------------------- ZONES & BEHAVIOUR --------------------------
    zones:
      # ---- 'prive' — Pool rooftop area (full frame) ----------------------
      prive:
        coordinates: 
          0.197,1,0.303,0.821,0.366,0.697,0.506,0.358,0.495,0.283,0.484,0.216,0.496,0.198,0.627,0.053,0.737,0.037,0.739,0.076,0.981,0.039,0.999,0.062,1,0.993,1,1
        loitering_time: 0        # Immediate alert if person appears
        inertia: 3               # Hold event for 3s after leaving
        objects: person
        filters:
          person:
            threshold: 0.05      # TRAINING MODE: very permissive
            min_area: 50       # TRAINING MODE: allow smaller boxes
            min_ratio: 0.01      # TRAINING MODE: extreme shape flexibility
            max_ratio: 10         # TRAINING MODE: extreme shape flexibility

    # ----------------------------- REVIEW RULES -----------------------------
    # review:
    #   alerts:
    #     required_zones: prive    # Only alert if event is inside the pool area
    #   detections:
    #     required_zones: prive    # Only keep detections when inside pool area

    # motion:
    #   mask: 
    #     0.157,0.996,0,1,0,0,0.592,0.007,0.323,0.303,0.32,0.352,0.41,0.324,0.471,0.304,0.481,0.347,0.382,0.586,0.343,0.673,0.292,0.768,0.242,0.862
  piscine_vue_toit_right:

    # ---------------------------- STREAM INPUTS -----------------------------
    ffmpeg:
      # hwaccel_args: []   # Uncomment if VA-API crashes on detect
      inputs:
        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_vue_sud_sub_right
          roles: [detect]
#        - path: rtsp://go2rtc:go2rtc@192.168.50.112:8556/reolink_duo_vue_sud_main
#          roles: [record, audio]

    # --------------------------- AUDIO (RECORD) -----------------------------
    audio:
      enabled: false              # Keep mic channel in recordings

    # --------------------------- DETECTION CORE -----------------------------
    detect:
      width: 768
      height: 432
      fps: 4                     # Slightly higher than 2 for more chances to catch poses

    # ------------------------------ OBJECTS --------------------------------
    objects:
      track: [person]
      filters:
        person:
          min_area: 50         # TRAINING MODE: catch small/distant figures
          max_area: 200000
          min_ratio: 0.01        # TRAINING MODE: extreme shape flexibility
          max_ratio: 10         # TRAINING MODE: extreme shape flexibility
          threshold: 0.05        # TRAINING MODE: very permissive

    # ------------------------------ SNAPSHOTS -------------------------------
    snapshots:
      enabled: true              # Use global settings for retention

    # --------------------------- ZONES & BEHAVIOUR --------------------------
    zones:
      # ---- 'prive' — Pool rooftop area (full frame) ----------------------
      prive:
        coordinates: 
          0,1,1,1,0.998,0.004,0.604,0,0.454,0.591,0.417,0.6,0.33,0.446,0.292,0.448,0.154,0.207,0.079,0.119,0,0.039
        loitering_time: 0        # Immediate alert if person appears
        inertia: 3               # Hold event for 3s after leaving
        objects: person
        filters:
          person:
            threshold: 0.05      # TRAINING MODE: very permissive
            min_area: 50       # TRAINING MODE: allow smaller boxes
            min_ratio: 0.01      # TRAINING MODE: extreme shape flexibility
            max_ratio: 10         # TRAINING MODE: extreme shape flexibility

    # ----------------------------- REVIEW RULES -----------------------------
    # review:
    #   alerts:
    #     required_zones: prive    # Only alert if event is inside the pool area
    #   detections:
    #     required_zones: prive    # Only keep detections when inside pool area

    # motion:
    #   mask: 
    #     0,0,0.571,0,0.485,0.288,0.439,0.524,0.435,0.568,0.413,0.565,0.347,0.444,0.299,0.45,0.265,0.391,0.18,0.237,0.029,0.055


version: 0.15-1               # Frigate config versioning (leave as‑is)

semantic_search:
  enabled: true               # Enable trial feature for faster clip search
  reindex: false              # True ➜ force rebuild of vector DB on startup
  model_size: small           # Options: small | medium | large – GPU RAM required

# ---------------------------------------------------------------------------
# END OF FILE – You can drop this yaml directly into ./config/frigate.yml
# ---------------------------------------------------------------------------
